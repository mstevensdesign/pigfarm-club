import React from "react";

type Props = {};

const DeadByDaylight = (props: Props) => {
  return (
    <div className="flex h-[calc(100vh-74px)] w-screen flex-col items-center justify-center">
      <h1 className="text-center">Dead By Daylight</h1>
    </div>
  );
};

export default DeadByDaylight;
