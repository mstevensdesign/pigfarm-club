import React from "react";

type Props = {};

const Deadlock = (props: Props) => {
  return (
    <div className="flex h-screen w-screen flex-col items-center justify-center">
      <h1 className="text-center">Deadlock</h1>
    </div>
  );
};

export default Deadlock;
